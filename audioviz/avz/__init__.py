"""avz — audio-reactive video renderer.

Takes an audio file, extracts musical features with librosa, paints frames with
numpy, and encodes a finished mp4 with ffmpeg. No GUI, no manual editing.
"""

__version__ = "0.1.0"
